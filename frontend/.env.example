# Apps Script Web App URL (from Deploy → Manage deployments in script.google.com).
# Leave unset during local development to run against built-in mock data.
VITE_API_URL=https://script.google.com/macros/s/REPLACE_WITH_YOUR_DEPLOYMENT_ID/exec
